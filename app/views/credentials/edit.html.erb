<h1>Credenciais TOTVS</h1>

<div class="card">
  <%= form_with model: @user, url: credentials_path, method: :patch, local: true do |f| %>
    <div>
      <%= f.label :totvs_username, "Usuário TOTVS" %>
      <%= f.text_field :totvs_username %>
    </div>
    <div>
      <%= f.label :totvs_password, "Senha TOTVS" %>
      <%= f.password_field :totvs_password, value: @user.totvs_password.presence %>
    </div>
    <div>
      <%= f.submit "Salvar" %>
    </div>
  <% end %>
</div>